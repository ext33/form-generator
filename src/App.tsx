import React, { FC } from "react"

import "./styles/root.scss"
import MainForm from "./components/main-form/MainForm"

const App: FC = () => {
    return (
        <div>
            <MainForm />
        </div>
    )
}

export default App
